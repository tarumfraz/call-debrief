{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "mkdir -p \"${CLAUDE_PLUGIN_ROOT}/debriefs\" && file_path=$(jq -r '.tool_input.file_path // empty') && if echo \"$file_path\" | grep -q 'debriefs/'; then echo \"[$(date '+%Y-%m-%d %H:%M')] Debrief saved: $file_path\" >> \"${CLAUDE_PLUGIN_ROOT}/debriefs/.log\"; fi"
          }
        ]
      }
    ]
  }
}
